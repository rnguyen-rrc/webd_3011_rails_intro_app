<div class="container mt-4">
  <h1 class="mb-4">Categories</h1>

  <table class="table align-middle">
    <thead>
      <tr>
        <th>Name</th>
        <th>Recipes</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @categories.each do |category| %>
        <tr>
          <td><%= category.name %></td>
          <td><%= category.recipes.count %></td>
          <td>
            <%= link_to "Show Recipes", category_path(category), class: "btn btn-outline-success" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>